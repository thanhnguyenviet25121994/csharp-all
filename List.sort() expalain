- Dùng cách ghi này để nhớ: 
ListSinhVien.Sort((min, max) => max.To.CompareTo(min.To)); => sort list sinh viên, khi so sánh 2 phần tử bất kỳ, sort từ max to min 
ListSinhVien.Sort((min, max) => min.To.CompareTo(max.To)); => ort list sinh viên, khi so sánh 2 phần tử bất kỳ, sort từ min to max
